import { useAnalytics, useSummary } from "@/lib/data";
import { PageHeader, DataState } from "@/components/ConsolePrimitives";
import { HistogramPanel, CompositionPanel } from "@/components/MissionCharts";

export default function AnalyticsWorkspace(){const analytics=useAnalytics(),summary=useSummary();const s=!summary.isError?summary.data:undefined;const mismatch=!!s&&!!analytics.data&&analytics.data.summary.total_objects!==s.total_objects;const a=!analytics.isError&&!mismatch?analytics.data:undefined;
  return <div data-testid="analytics-page" className="fade-enter"><PageHeader eyebrow="INTELLIGENCE / CATALOG DISTRIBUTIONS" title="Analytics" description="Distributions calculated by the scientific backend. Counts share the same catalog summary used throughout ORBIS." action={<div data-testid="analytics-total-count" className="font-mono text-lg text-cyan-100">{s?.total_objects.toLocaleString()??"—"}<span className="ml-2 text-[9px] text-slate-500">CATALOG OBJECTS</span></div>}/><DataState error={analytics.isError||summary.isError||mismatch} loading={analytics.isLoading||summary.isLoading} message="LOADING BACKEND DISTRIBUTIONS" retry={()=>{analytics.refetch();summary.refetch();}}/>{mismatch?<p data-testid="analytics-integrity-error" className="mb-4 text-xs text-red-300">DATA INTEGRITY ALERT — analytics and summary disagree. Charts withheld.</p>:null}<div className="grid gap-4 xl:grid-cols-3">{s?<><CompositionPanel id="type" title="Catalog composition" data={[{label:"Satellite",count:s.satellites,color:"#70c8d9"},{label:"Debris",count:s.debris,color:"#d9a361"}]}/><CompositionPanel id="decision" title="Analysis decisions" data={[{label:"FAST",count:s.fast,color:"#81bbaa"},{label:"DEEP",count:s.deep,color:"#d9ac6d"},{label:"INSUFFICIENT_DATA",count:s.insufficient_data,color:"#596778"}]}/><CompositionPanel id="ml" title="Model reliability" data={[{label:"RELIABLE",count:s.reliable,color:"#6ca7d0"},{label:"LOW_CONFIDENCE",count:s.low_confidence,color:"#cdb181"}]}/></>:null}</div><div className="mt-4 grid gap-4 md:grid-cols-2"><HistogramPanel id="aci_histogram" title="ACI distribution" data={a?.aci_histogram||[]}/><HistogramPanel id="model_confidence_histogram" title="Model confidence distribution" data={a?.model_confidence_histogram||[]} color="#86b9ab"/><HistogramPanel id="data_age_histogram" title="Data age distribution" data={a?.data_age_histogram||[]} color="#d6a061" unit="DAYS"/><HistogramPanel id="trajectory_consistency_histogram" title="Trajectory consistency" data={a?.trajectory_consistency_histogram||[]} color="#85a9cb"/></div><p data-testid="analytics-source-disclaimer" className="mt-4 text-[10px] leading-5 text-slate-500">ACI and model confidence are reliability indicators, not collision probabilities. Histogram bins with missing values are excluded by the backend; missing data is not replaced.</p></div>;
}