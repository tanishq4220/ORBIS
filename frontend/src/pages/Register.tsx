import { useState } from "react";
import type { FormEvent } from "react";
import { useMutation, useQueryClient } from "@tanstack/react-query";
import { Link, useNavigate } from "react-router-dom";
import { apiPost } from "@/lib/api";
import type { AuthUser } from "@/lib/orbis";
export default function Register() { const [name, setName] = useState(""); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [error, setError] = useState(""); const navigate = useNavigate(); const qc = useQueryClient(); const register = useMutation({ mutationFn: () => apiPost<AuthUser>("/auth/register", { name, email, password }), onSuccess: (user) => { qc.setQueryData(["auth-me"], user); navigate("/dashboard", { replace: true }); }, onError: () => setError("Registration failed. Check your details.") }); const submit = (event: FormEvent) => { event.preventDefault(); setError(""); register.mutate(); }; return <div data-testid="register-page" className="grid min-h-screen place-items-center bg-[#050811] px-5"><div className="w-full max-w-md rounded-xl border border-slate-800 bg-[#0a101d] p-8"><div data-testid="register-kicker" className="font-mono text-[10px] tracking-[0.18em] text-cyan-300">ORBIS / NEW OPERATOR</div><h1 data-testid="register-heading" className="mt-3 font-heading text-3xl font-bold text-white">Create access</h1><p data-testid="register-description" className="mt-2 text-sm text-slate-500">Your name will be used across the mission-control profile.</p><form data-testid="register-form" onSubmit={submit} className="mt-7 space-y-4"><input data-testid="register-name-input" required minLength={2} placeholder="Operator name" value={name} onChange={(e) => setName(e.target.value)} className="h-11 w-full rounded border border-slate-800 bg-slate-950 px-3 text-sm text-white outline-none" /><input data-testid="register-email-input" required type="email" placeholder="operator@domain" value={email} onChange={(e) => setEmail(e.target.value)} className="h-11 w-full rounded border border-slate-800 bg-slate-950 px-3 text-sm text-white outline-none" /><input data-testid="register-password-input" required minLength={8} type="password" placeholder="Minimum 8 characters" value={password} onChange={(e) => setPassword(e.target.value)} className="h-11 w-full rounded border border-slate-800 bg-slate-950 px-3 text-sm text-white outline-none" />{error ? <div data-testid="register-error" className="text-xs text-red-300">{error}</div> : null}<button data-testid="register-submit-button" className="h-11 w-full rounded bg-cyan-400 font-bold text-slate-950 hover:bg-cyan-300">CREATE OPERATOR ACCOUNT</button></form><Link data-testid="register-back-login-link" to="/login" className="mt-6 block text-center text-xs text-slate-500 hover:text-cyan-300">Return to sign in</Link></div></div>; }