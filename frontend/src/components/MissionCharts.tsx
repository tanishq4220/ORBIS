import { Bar, BarChart, CartesianGrid, Cell, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import type { Histogram } from "@/lib/orbis";
import { Panel } from "./ConsolePrimitives";

const tooltip={background:"#101c2a",border:"1px solid #2a3d4f",borderRadius:3,fontSize:11,color:"#e2e8f0"};
export function HistogramPanel({title,data,color="#66c4d5",unit="",id}:{title:string;data:Histogram[];color?:string;unit?:string;id:string}){
  return <Panel title={title} eyebrow={`BACKEND DISTRIBUTION ${unit?`/ ${unit}`:""}`}><div data-testid={`analytics-chart-${id}`} className="h-[215px] min-w-0">{data.length?<ResponsiveContainer width="100%" height="100%"><BarChart data={data} margin={{top:12,right:5,left:-15,bottom:0}}><CartesianGrid stroke="#1c2b3a" vertical={false}/><XAxis dataKey="bin_start" axisLine={false} tickLine={false} tickFormatter={n=>Number(n).toFixed(1)} tick={{fill:"#7a8fa4",fontSize:9}} minTickGap={25}/><YAxis axisLine={false} tickLine={false} tick={{fill:"#7a8fa4",fontSize:9}}/><Tooltip contentStyle={tooltip} cursor={{fill:"#24344755"}} labelFormatter={n=>`From ${Number(n).toFixed(3)} ${unit}`}/><Bar name="Objects" dataKey="count" fill={color} radius={[1,1,0,0]} isAnimationActive={false}/></BarChart></ResponsiveContainer>:<div className="grid h-full place-items-center font-mono text-[10px] text-slate-500">DATA UNAVAILABLE</div>}</div></Panel>;
}
export function CompositionPanel({title,data,id}:{title:string;data:{label:string;count:number;color:string}[];id:string}){return <Panel title={title} eyebrow="RECONCILED / BACKEND SUMMARY"><div data-testid={`analytics-${id}-distribution`} className="grid grid-cols-[125px_minmax(0,1fr)] items-center gap-3"><div className="h-32 min-w-0"><ResponsiveContainer width="100%" height="100%"><PieChart><Pie data={data} dataKey="count" nameKey="label" innerRadius={40} outerRadius={53} paddingAngle={2} stroke="none" isAnimationActive={false}>{data.map(d=><Cell key={d.label} fill={d.color}/>)}</Pie><Tooltip contentStyle={tooltip}/></PieChart></ResponsiveContainer></div><div className="space-y-3">{data.map(d=><div key={d.label} data-testid={`composition-${id}-${d.label.toLowerCase().replaceAll("_","-")}`} className="flex items-center justify-between gap-2 text-[9px]"><span className="flex items-center gap-1.5 text-slate-400"><i className="h-1 w-1 rounded-full" style={{background:d.color}}/>{d.label.replaceAll("_"," ")}</span><span className="font-mono text-slate-200">{d.count.toLocaleString()}</span></div>)}</div></div></Panel>;}